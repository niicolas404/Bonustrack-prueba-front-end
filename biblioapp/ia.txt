--etapa 1--
prompt: analiza App.jsx e importa el array de libros.js, ya que necesito que se muestre solamente la información de los libros y elimines de paso todo lo referente a la pagina por defecto de react. Es sumamente importante que no quede rastro de aquellos datos en otros archivos, pero siempre manteniendo el cuidado de NO borrar código que corresponda al proyecto en el que trabajaremos. Solo elimina lo referente a la pagina react para que el código quede limpio y haz los cambios que pedí sobre el array. 


resultado: cumplió con las instrucciones de manera eficiente

utilidad: Muy útil.


--etapa 2--
prompt: utiliza LibroCard.jsx para que el componente reciba de libros.js por props el título del libro, editorial, anio, estado, resumen, autores. Muestra la información e manera ordenada y aplica estilos diferenciado según el estado: verde para "disponible", amarillo para "prestado" y gris para "reservado". Es sumamente importante que definas los tipos de las props con PropTypes y entrega valores por defecto para evitar errores cuando falte un dato.

renderiza el texto siempre como contenido jsx normal. No uses dangerouslySetInnerHTML. Por ultimo, no dejes credenciales, claves ni datos sensibles escritos en el código.

prompt 2: Veo que dejaste a #file:LibroCrad.jsx sin utilidad a pesar de ser un componente que actua para que su función funcione de manera independiente. ¿Puedes hacer que el componente tenga una funcion real dentro del proyecto como corresponde?

resultado: el objetivo fue logrado, pero dejó a librocard sin ajustes, por lo que se opta por otro prompt para poder arreglarlo.



utilidad: Útil



--etapa 3--
prompt: Necesito que ListaLibros.jsx reciba el array de libros por props. Este array debe recorrer con .map() y renderizar un libroCard por cada libro. También se debes asignar la prop key usando el id de cada elemento (No uses el índice del array) por ultimo, en la App.jsx , renderiza #file:ListaLibros.jsx pasándole los datos importados.

resultado: el resultado cumplió con lo estipulado en el prompt

utilidad: fue util


--etapa 4--
prompt: vamos a aplicar un estado y filtro seguro. Necesito que crees botones o un selector para filtrar por el estado: "todos", "disponible", "prestado", reservado", desde #file:FiltroEstado.jsx. En #file:App.jsx usa "useState" para guardar el filtro seleccionado y un campo de búsqueda por titulo. Aplica ambos filtros sobre el array antes de pasarlo a #file:ListaLibros.jsx y haz que se renderice de manera condicional para mostrar el mensaje "no hay libros que coincidan" cuando el resultado quede vacío,

el campo de búsqueda debe ser un componente controlado (su valor vive en el estado de la react)
por ultimo valida y normaliza el texto de búsqueda antes de usarlo para no confiar ciegamente en la entrada de usuario.

resultado: el resultado cumplió, el filtro de búsqueda funciona como corresponde.

utilidad: muy util.


--etapa 5--
prompt: Destaquemos novedades. Resalta visualmente los elementos con esNovedad: true (ejemplo: una etiqueta "nuevo" o un borde distinto), además agrega un contador que muestre cuántos libros son novedad.

resultado: la novedad de "nuevo" cumple, adicionalmente se genera un contador donde se ven cuantos libros son nuevos.

utilidad: muy útil.



--sección de mejoras--
prompt: vamos a hacer una mejora de estilos css. usa app.css para mejorar los colores de la pagina y la legibilidad de las letras. Quiero letras en negritas con fondos blancos. El color de la pagina de fondo puede ser de color beige claro para otorgar una apariencia minimalista. Los botones de "nuevo" deben aparecer en rojo. el estado de cada libro puede conservar su color actual. Haz que el encabezado se vea atractivo y borrra el pararfo de "aqupi se muestra solo la informacióm de los libros desde el array de datos" lo cual aparece en la paghina de subtitulo y lo cual no debería.

resultado. se pudo conseguir que los componentes funcionen por separado de manera eficiente. Adicionalmente se hace una mejora de estilo para mayor legibilidad.

utildiad. muy util.